# Presentations

Веб-приложение для просмотра интерактивных презентаций с поддержкой различных типов контента: текст, код с подсветкой синтаксиса и списки. 

## 🤖 Разработка с использованием AI

Этот проект был разработан с помощью [Cursor](https://cursor.sh) — IDE со встроенным искусственным интеллектом (на базе моделей OpenAI).

## 🚀 Возможности

- 🎨 **Современный UI** - темная тема с градиентами и анимациями
- ⌨️ **Клавиатурная навигация** - управление с клавиатуры
- 💻 **Подсветка синтаксиса** - автоматическая подсветка кода через highlight.js
- 📊 **Гибкая структура слайдов** - поддержка одно- и двухколоночного макета
- 🎯 **Индикаторы слайдов** - визуальная навигация по слайдам

## 📁 Структура проекта

```
Презентации/
├── index.html              # Главная HTML страница
├── styles.css              # Стили приложения
├── js/                     # JavaScript модули
│   ├── main.js            # Главный модуль инициализации
│   ├── state.js           # Управление состоянием приложения
│   ├── load.js            # Загрузка данных презентаций
│   ├── render.js          # Рендеринг UI компонентов
│   ├── navigation.js      # Навигация по слайдам
│   └── presentation.js    # Управление презентациями
└── presentations/          # Презентации в формате JSON
    ├── list.json          # Список всех презентаций
    ├── lecture_1.json     # Примеры презентаций
    └── ...
```

## 🛠️ Установка и запуск

### Требования

- Современный веб-браузер с поддержкой ES6 модулей
- Локальный веб-сервер (для работы с модулями)

### Быстрый старт

1. Клонируйте или скачайте проект
2. Запустите локальный веб-сервер:

   **Python:**
   ```bash
   python -m http.server 8000
   ```

   **Node.js (http-server):**
   ```bash
   npx http-server
   ```

   **VS Code Live Server:**
   - Установите расширение "Live Server"
   - Откройте `index.html` и нажмите "Go Live"

3. Откройте браузер и перейдите по адресу `http://localhost:8000`

## 📖 Использование

### Навигация

- **Кнопки навигации:**
  - `←` - Предыдущий слайд
  - `→` - Следующий слайд
  - `|||` - Вернуться на главную страницу

- **Клавиатура:**
  - `→` или `Пробел` - Следующий слайд
  - `←` - Предыдущий слайд
  - `Home` - Вернуться на главную страницу

- **Индикаторы слайдов:**
  - Клик по точке в правой панели для перехода к конкретному слайду

## 📝 Формат данных презентаций

### Список презентаций (`presentations/list.json`)

```json
{
  "presentations": [
    {
      "file": "lecture_1.json",
      "title": "Название презентации"
    }
  ]
}
```

### Структура слайда

Презентация - это JSON файл со следующей структурой:

```json
{
  "slides": [
    {
      "title": "Заголовок слайда",
      "sub_title": "Подзаголовок (опционально)",
      "content": [
        {
          "type": "text",
          "content": "Текст контента"
        },
        {
          "type": "code",
          "language": "javascript",
          "content": "console.log('Hello World');"
        },
        {
          "type": "list",
          "items": [
            "Пункт 1",
            "Пункт 2",
            "Пункт 3"
          ]
        }
      ]
    }
  ]
}
```

### Двухколоночный макет

Для создания двухколоночного слайда используйте `left_column` и `right_column`:

```json
{
  "title": "Двухколоночный слайд",
  "left_column": [
    {
      "type": "text",
      "content": "Контент левой колонки"
    }
  ],
  "right_column": [
    {
      "type": "code",
      "language": "javascript",
      "content": "const example = 'code';"
    }
  ]
}
```

### Типы контента

1. **text** - Обычный текст
   ```json
   {
     "type": "text",
     "content": "Ваш текст здесь"
   }
   ```

2. **code** - Блок кода с подсветкой синтаксиса
   ```json
   {
     "type": "code",
     "language": "javascript",
     "content": "const x = 10;"
   }
   ```
   Поддерживаемые языки: `javascript`, `python`, `html`, `css`, `json` и другие (см. [highlight.js](https://highlightjs.org/))

3. **list** - Маркированный список
   ```json
   {
     "type": "list",
     "items": [
       "Элемент 1",
       "Элемент 2",
       "Элемент 3"
     ]
   }
   ```

## 🏗️ Архитектура

Проект использует модульную архитектуру ES6:

- **`state.js`** - Централизованное управление состоянием приложения
- **`load.js`** - Загрузка данных из JSON файлов
- **`render.js`** - Рендеринг UI компонентов и слайдов
- **`navigation.js`** - Логика навигации по слайдам
- **`presentation.js`** - Управление жизненным циклом презентаций
- **`main.js`** - Инициализация приложения и привязка событий

## 🎨 Кастомизация

### Изменение цветовой схемы

Отредактируйте CSS переменные в `styles.css`:

```css
:root {
    --color-title: #4da6ff;
    --color-subtitle: #3d8fcc;
    --color-text: #f0f0f0;
    --bg-page: #1e1e1e;
    --bg-slide-start: #2d2d2d;
    --bg-slide-end: #3d3d3d;
}
```

### Изменение темы подсветки кода

Замените ссылку на тему highlight.js в `index.html`:

```html
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/github-dark.min.css">
```

Доступные темы: https://github.com/highlightjs/highlight.js/tree/main/src/styles

## 📦 Зависимости

- [highlight.js](https://highlightjs.org/) - Подсветка синтаксиса кода (подключается через CDN)

## 🌐 Совместимость

- Chrome/Edge (последние версии)
- Firefox (последние версии)
- Safari (последние версии)
- Мобильные браузеры (iOS Safari, Chrome Mobile)

## 📄 Лицензия

Проект для образовательных целей.

## 🤝 Вклад

Приветствуются предложения по улучшению! Создавайте issues и pull requests.

## 📧 Контакты

Для вопросов и предложений создавайте issues в репозитории проекта.

